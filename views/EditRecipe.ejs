<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add a Recipe</title>
    <link rel="stylesheet" type="text/css" href="/addrecipe.css">
</head>

<body>
    <nav class="navbar">
        <div class="container">
            <h1 class="logo">Recipe Notes</h1>
            <ul class="nav-links">
                <li><a href="#">Home</a></li>
                <li><a href="#">Add Recipe</a></li>
                <li><a href="#">Contact</a></li>
            </ul>
        </div>
    </nav>

    <div class="container">
        <h2>Add a New Recipe</h2>
        <form class="recipe-form" action="/recipes" method="post">
            <div class="form-group">
                <label for="title">Recipe Title</label>
                <input type="text" value="<%= recipe[0].title %>" id="title" name="title" required>
            </div>
            <div class="form-group">
                <label for="ingredients">Ingredients</label>
                <textarea id="ingredients" name="ingredients" value="<%= recipe[0].ingredients %>"
                    placeholder="<%= recipe[0].ingredients %>" rows="5" required><%= recipe[0].ingredients %></textarea>
            </div>
            <div class="form-group">
                <label for="instructions">Instructions</label>
                <textarea id="instructions" name="instructions" value="<%= recipe[0].instructions %>"
                    placeholder="<%= recipe[0].instructions %>" rows="5"
                    required><%= recipe[0].instructions %></textarea>
            </div>
            <div class="form-group">
                <label for="image">Upload Image</label>
                <input type="file" id="image" name="image" accept="image/*">
            </div>
            <button type="submit">Add Recipe</button>
        </form>
    </div>
</body>

</html>